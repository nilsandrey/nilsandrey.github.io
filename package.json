{
  "name": "nilsandrey-com-phase-1",
  "version": "0.1.0",
  "private": true,
  "description": "Validation tooling for the static Phase 1 Cloudflare Pages deployment.",
  "type": "module",
  "scripts": {
    "check": "npm run check:links && npm run test:e2e",
    "check:links": "node scripts/check-internal-links.mjs",
    "serve": "http-server . --host 127.0.0.1 --port 4173 --silent",
    "test:e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.54.0",
    "http-server": "^14.1.1"
  }
}
